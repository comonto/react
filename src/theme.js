import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1976d2',
      // main: '#FF0000',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;
